# Supabase connection
NEXT_PUBLIC_SUPABASE_URL=https://<your-project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...

# OpenFeedback project (UUID from the `projects` table)
NEXT_PUBLIC_OPENFEEDBACK_PROJECT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Server-only: HMAC secret for signing requests (must match `projects.hmac_secret`)
OPENFEEDBACK_HMAC_SECRET=your-hmac-secret-here
